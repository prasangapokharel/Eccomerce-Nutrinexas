PHP session files are stored here when using file-based session storage.

This folder is created automatically if missing.

Note: Files may be purged by `SessionHandler::cleanOldSessions()` after ~30 days.